name: Deploy to S3

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Install AWS CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y awscli

      - name: Configure AWS CLI
        run: |
          aws configure set aws_access_key_id "AKIA6ODU4LUD5Y3T6E62"
          aws configure set aws_secret_access_key "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"
          aws configure set default.region "us-east-1"

      - name: Deploy to S3
        run: |
          aws s3 sync . s3://aidancorcoran-resume
